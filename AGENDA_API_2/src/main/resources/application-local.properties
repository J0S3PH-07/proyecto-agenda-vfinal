server.port=8085
spring.datasource.url=jdbc:mysql://localhost:3307/room_reservation_system
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# URLs Configuration (Swagger testing)
app.frontend.url=http://localhost:8081
app.backend.url=http://localhost:8085

# OAuth2 Google Configuration (Dummy values for local testing - only Swagger)
spring.security.oauth2.client.registration.google.client-id=local-dummy-client-id
spring.security.oauth2.client.registration.google.client-secret=local-dummy-client-secret

# AWS Cognito Configuration (Dummy values for local testing)
aws.cognito.userPoolId=local-dummy-pool-id
aws.cognito.clientId=local-dummy-client-id

